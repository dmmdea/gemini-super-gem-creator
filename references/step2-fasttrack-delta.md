# Step 2: GENERATE — Fast Track Delta

> **Load AFTER `step2-generate.md`.** This file contains ONLY Fast Track overrides.

## Fast Track Overrides

Fast Track produces ALL of the following:
- Full 4-pillar system prompt at **~2,000-char target** (NOT lean 1,500 — inline context compensates for absent §5–§10)
- Top 3 constraints embedded directly in the prompt (normally in §2 of memory hub)
- Top 3 never-recommends embedded directly in the prompt (normally in §8)
- Key domain context inline that would normally live in §3 (current state)
- All Harness Proxy behavioral rules (prompt-level, zero extra file cost)
- Memory hub **§0–§4 starter** (Drive folder IS created — just lean)
- Archive file, Progress file, Heuristics Pool (created but empty)
- Test 5 mandatory
- Gem Registry entry

## Fast Track Does NOT Include

- §5–§10 memory hub sections
- NotebookLM notebooks (0 notebooks)
- Tri-file architecture (never generated)

## Fast Track Memory Hub

Fill **§0–§4 only:** §0 How to Use This File, §1 Identity, §2 Hard Constraints, §3 Current State Snapshot, §4 Decision Log. Top 3 never-recommends go inline in the system prompt (§8 is excluded).
